const MessageEmbed = require("discord.js").MessageEmbed;

const coin = require("./coin")
const User = require("../models/User")
const Item = require("../models/Item")

const fishManager = async (message, args) => {
    
}

module.exports = {
    fishManager
}